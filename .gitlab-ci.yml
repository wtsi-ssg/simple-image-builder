variables:
 OS_NO_CACHE: "True"
 COMPUTE_API_VERSION: "1.1"
 no_proxy: ",172.31.0.18" 
 OS_CLOUDNAME: "overcloud"
 OS_AUTH_URL: "http://openstack-gamma.internal.sanger.ac.uk:5000/v2.0/"
 NOVA_VERSION: "1.1"
 PACKER_BIN: "/home/gitlab-runner/packer-0.10.1"
 OS_BASE_IMAGE_TRUSTY: "8f50f1c3-b094-492b-8c73-191a8f1f5582"
 IMAGE_NAME_TRUSTY: "trusty-WTSI-example"
 OS_BASE_IMAGE_XENIAL: "002ca5be-3934-43d3-9d2c-eda816b57019"
 IMAGE_NAME_XENIAL: "xenial-WTSI-example"
 OS_BASE_IMAGE_CENTOS7: "62313395-5876-439c-9a36-86839ad27195"
 IMAGE_NAME_CENTOS7: "centos7-WTSI-example"
 OS_SECURITY_GRP: "ssh"
 proxy: "http://wwwcache.sanger.ac.uk:3128"


stages:
 - init_openstack
 - build_openstack
 - cleanup_build
 - test_openstack
 - cleanup_test
 - store

validate_template_trusty_openstack:
  stage: init_openstack
  tags: 
   - packer
  script:
   - export IMAGE_NAME="$IMAGE_NAME_TRUSTY"
   - export OS_BASE_IMAGE="$OS_BASE_IMAGE_TRUSTY"
   - ./create_image.py validate -tf template-openstack-ubuntu.json -vf variables.json

validate_template_xenial_openstack:
  stage: init_openstack
  tags:
   - packer
  script:
   - export IMAGE_NAME="$IMAGE_NAME_XENIAL"
   - export OS_BASE_IMAGE="$OS_BASE_IMAGE_XENIAL"
   - ./create_image.py validate -tf template-openstack-ubuntu.json -vf variables.json

validate_template_centos7_openstack:
  stage: init_openstack
  tags:
   - packer
  script:
   - export IMAGE_NAME="$IMAGE_NAME_CENTOS7"
   - export OS_BASE_IMAGE="$OS_BASE_IMAGE_CENTOS7"
   - ./create_image.py validate -tf template-openstack-centos.json -vf variables.json

build_openstack_trusty:
 stage: build_openstack
 tags:
  - packer
 script:
  - export IMAGE_NAME="$IMAGE_NAME_TRUSTY"
  - export OS_BASE_IMAGE="$OS_BASE_IMAGE_TRUSTY"
  - ./create_image.py build -d local -tf template-openstack-ubuntu.json -vf variables.json -p openstack -o "${IMAGE_NAME}"_"${CI_BUILD_REF}"

build_openstack_xenial:
 stage: build_openstack
 tags:
  - packer
 script:
  - export IMAGE_NAME="$IMAGE_NAME_XENIAL"
  - export OS_BASE_IMAGE="$OS_BASE_IMAGE_XENIAL"
  - ./create_image.py build -d local -tf template-openstack-ubuntu.json -vf variables.json -p openstack -o "${IMAGE_NAME}"_"${CI_BUILD_REF}"

build_openstack_centos7:
 stage: build_openstack
 tags:
  - packer
 script:
  - export IMAGE_NAME="$IMAGE_NAME_CENTOS7"
  - export OS_BASE_IMAGE="$OS_BASE_IMAGE_CENTOS7"
  - ./create_image.py build -d local -tf template-openstack-centos.json -vf variables.json -p openstack -o "${IMAGE_NAME}"_"${CI_BUILD_REF}"

cleanup_openstack_build:
  stage: cleanup_build
  tags:
   - packer
  script:
   - export IMAGE_NAME="$IMAGE_NAME_CENTOS7"
   - ./remove_failed_builds.py
   - export IMAGE_NAME="$IMAGE_NAME_TRUSTY"
   - ./remove_failed_builds.py
   - export IMAGE_NAME="$IMAGE_NAME_XENIAL"
   - ./remove_failed_builds.py
  when: on_failure


test_openstack_no_disk_trusty:
  stage: test_openstack
  tags:
   - openstack
   - kitchen
  script:
  - export IMAGE_NAME="$IMAGE_NAME_TRUSTY"
  - export OS_BASE_IMAGE=$(openstack image list -f value | grep -w "${IMAGE_NAME}"_"${CI_BUILD_REF}" |  cut -d " " -f 1)
  - export MODE=default
  - export IMAGE_USERNAME="ubuntu"
  - export PLATFORM="ubuntu-14.04"
  - export http_proxy=""
  - export http_proxys=""
  - export KEYPAIR=$( (echo -n $PLATFORM ; echo -n $MODE ; date +%s-%N ) | md5sum | cut -d " " -f 1)
  - ./kitchen_wrapper.sh

test_openstack_no_disk_xenial:
  stage: test_openstack
  tags:
   - openstack
   - kitchen
  script:
  - export IMAGE_NAME="$IMAGE_NAME_XENIAL"
  - export OS_BASE_IMAGE=$(openstack image list -f value | grep -w "${IMAGE_NAME}"_"${CI_BUILD_REF}" |  cut -d " " -f 1)
  - export MODE=default
  - export IMAGE_USERNAME="ubuntu"
  - export PLATFORM="ubuntu-16.04"
  - export http_proxy=""
  - export http_proxys=""
  - export KEYPAIR=$( (echo -n $PLATFORM ; echo -n $MODE ; date +%s-%N )| md5sum | cut -d " " -f 1)
  - ./kitchen_wrapper.sh

test_openstack_no_disk_centos7:
  stage: test_openstack
  tags:
   - openstack
   - kitchen
  script:
  - export IMAGE_NAME="$IMAGE_NAME_CENTOS7"
  - export OS_BASE_IMAGE=$(openstack image list -f value | grep -w "${IMAGE_NAME}"_"${CI_BUILD_REF}" |  cut -d " " -f 1)
  - export MODE=default
  - export PLATFORM="centos-7.0"
  - export IMAGE_USERNAME="centos"
  - export http_proxy=""
  - export http_proxys=""
  - export KEYPAIR=$( (echo -n $PLATFORM ; echo -n $MODE ; date +%s-%N ) | md5sum | cut -d " " -f 1)
  - ./kitchen_wrapper.sh

test_openstack_disk_trusty:
  stage: test_openstack
  tags:
   - openstack
   - kitchen
  script:
  - export IMAGE_NAME="$IMAGE_NAME_TRUSTY"
  - export OS_BASE_IMAGE=$(openstack image list -f value | grep -w "${IMAGE_NAME}"_"${CI_BUILD_REF}" |  cut -d " " -f 1)
  - export MODE=volume
  - export IMAGE_USERNAME="ubuntu"
  - export PLATFORM="ubuntu-14.04"
  - export http_proxy=""
  - export http_proxys=""
  - export KEYPAIR=$( (echo -n $PLATFORM ; echo -n $MODE ; date +%s-%N ) | md5sum | cut -d " " -f 1)
  - ./kitchen_wrapper.sh

test_openstack_disk_xenial:
  stage: test_openstack
  tags:
   - openstack
   - kitchen
  script:
  - export IMAGE_NAME="$IMAGE_NAME_XENIAL"
  - export OS_BASE_IMAGE=$(openstack image list -f value | grep -w "${IMAGE_NAME}"_"${CI_BUILD_REF}" |  cut -d " " -f 1)
  - export MODE=volume
  - export IMAGE_USERNAME="ubuntu"
  - export PLATFORM="ubuntu-16.04"
  - export http_proxy=""
  - export http_proxys=""
  - export KEYPAIR=$( (echo -n $PLATFORM ; echo -n $MODE ; date +%s-%N ) | md5sum | cut -d " " -f 1)
  - ./kitchen_wrapper.sh

test_openstack_disk_centos7:
  stage: test_openstack
  tags:
   - openstack
   - kitchen
  script:
  - export IMAGE_NAME="$IMAGE_NAME_CENTOS7"
  - export OS_BASE_IMAGE=$(openstack image list -f value | grep -w "${IMAGE_NAME}"_"${CI_BUILD_REF}" |  cut -d " " -f 1)
  - export MODE=volume
  - export IMAGE_USERNAME="centos"
  - export PLATFORM="centos-7.0"
  - export http_proxy=""
  - export http_proxys=""
  - export KEYPAIR=$( (echo -n $PLATFORM ; echo -n $MODE ; date +%s-%N ) | md5sum | cut -d " " -f 1)
  - ./kitchen_wrapper.sh

cleanup_openstack_test:
  stage: cleanup_build
  tags:
   - packer
  script:
   - export IMAGE_NAME="$IMAGE_NAME_CENTOS7"
   - ./remove_failed_builds.py
   - export IMAGE_NAME="$IMAGE_NAME_TRUSTY"
   - ./remove_failed_builds.py
   - export IMAGE_NAME="$IMAGE_NAME_XENIAL"
   - ./remove_failed_builds.py

  when: on_failure

store_openstack_image:
  stage: store
  tags:
  - packer
  script:
  - export IMAGE_NAME="$IMAGE_NAME_CENTOS7"
  - ./cleanup.py -dt "${IMAGE_NAME}"_"${CI_BUILD_REF}" "${IMAGE_NAME}"
  - export IMAGE_NAME="$IMAGE_NAME_TRUSTY"
  - ./cleanup.py -dt "${IMAGE_NAME}"_"${CI_BUILD_REF}" "${IMAGE_NAME}"
  - export IMAGE_NAME="$IMAGE_NAME_XENIAL"
  - ./cleanup.py -dt "${IMAGE_NAME}"_"${CI_BUILD_REF}" "${IMAGE_NAME}"
